# Digital Marketing Course Payment System

## Setup
1. Navigate to backend folder:
   ```
   cd backend
   npm install
   node server.js
   ```
2. Open `frontend/index.html` in browser for landing page.
3. Open `frontend/admin.html` for admin panel.
4. Transactions are stored in `transactions.json`.
